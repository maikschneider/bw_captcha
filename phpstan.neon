includes:
   - vendor/saschaegerer/phpstan-typo3/extension.neon
   - phpstan-baseline.neon

parameters:
    parallel:
        maximumNumberOfProcesses: 5

    level: 8

    bootstrapFiles:
        - vendor/autoload.php

    paths:
        - Classes
        - Configuration

    excludePaths:
